// Removed — superseded by LibraryView, which now joins per-user reading
// progress on top of the shared library list. Kept as a placeholder so older
// imports (none in the current tree) wouldn't break the build.
export default function MyBooksView() {
  return null;
}
